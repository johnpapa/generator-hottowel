language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-hottowel' ]; then cd .. && eval "mv $currentfolder generator-hottowel" && cd generator-hottowel; fi

